sudo: false
language: node_js
node_js:
  - "7"
services:
  - mongodb
# until travis has mongodb > 3
addons:
  apt:
    sources:
    - mongodb-3.0-precise
    packages:
    - mongodb-org-server
script:
  - yarn coverage
after_success:
  - bash <(curl -s https://codecov.io/bash)
